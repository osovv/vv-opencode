<GraceDeployment graceVersion="4.0">
  <Applicability>applicable</Applicability>
  <Target>Public npm package @osovv/vv-opencode with Bun CLI binary vvoc plus OpenCode server and TUI plugin exports.</Target>
  <StabilityPosture>Version 1.0.0 is the daily-driver baseline for the curated vvoc workflow. The release treats setup commands, public package exports, canonical vvoc schema v3, managed skill names, and spec artifact layout as compatibility surfaces unless future release notes explicitly document a breaking change.</StabilityPosture>
  <PackageFiles>
    <File>dist</File>
    <File>schemas</File>
    <File>templates</File>
    <File>README.md</File>
    <File>CHANGELOG.md</File>
  </PackageFiles>
  <Exports>
    <Export>@osovv/vv-opencode</Export>
    <Export>@osovv/vv-opencode/plugins/guardian</Export>
    <Export>@osovv/vv-opencode/plugins/hashline-edit</Export>
    <Export>@osovv/vv-opencode/plugins/model-roles</Export>
    <Export>@osovv/vv-opencode/plugins/system-context-injection</Export>
    <Export>@osovv/vv-opencode/plugins/workflow</Export>
    <Export>@osovv/vv-opencode/plugins/secrets-redaction</Export>
    <Export>@osovv/vv-opencode/tui</Export>
  </Exports>
  <InstallAndSync>
    <Global>vvoc install and vvoc sync may write OpenCode runtime and TUI config under $XDG_CONFIG_HOME/opencode and vvoc config under $XDG_CONFIG_HOME/vvoc.</Global>
    <Project>Project scope writes OpenCode runtime/TUI files only inside .opencode and vvoc files only inside .vvoc under the selected project root.</Project>
    <Launch>vvoc launch pins OPENCODE_CONFIG and VVOC_CONFIG and conditionally pins an existing selected tui.json(c) through OPENCODE_TUI_CONFIG for deterministic sandboxed OpenCode startup.</Launch>
  </InstallAndSync>
  <ReleaseGates>
    <Command>bun run typecheck</Command>
    <Command>bun run lint</Command>
    <Command>bun run fmt:check</Command>
    <Command>bun test</Command>
    <Command>bun run build</Command>
    <Command>bun run pack:check</Command>
    <Command>bun run release:check</Command>
  </ReleaseGates>
  <Publishing>
    <Policy>Publishing is exact-commit and CI-gated through an explicit publish.yml workflow dispatch; normal branch and tag pushes never publish.</Policy>
    <Workflow>.github/workflows/publish.yml checks out the requested release SHA, verifies version and existing tag/package identity, runs release consistency, typecheck, lint, format, tests, and pack checks, then publishes to npm with read-only repository contents access.</Workflow>
    <LocalBump>bun run release:bump wraps npm version, AI changelog summary generation, schema id patching, release checks, release commit creation, branch-only push, publish.yml dispatch with exact version/SHA inputs, CI waiting, bounded npm metadata retry and gitHead verification, then authenticated local annotated-tag push and GitHub Release creation.</LocalBump>
    <TrustedPublishing>The workflow uses npm provenance/trusted publishing through id-token permissions.</TrustedPublishing>
    <NormalPushes>Normal branch pushes must not publish the package.</NormalPushes>
    <Recovery>A rerun may skip npm publication only when the existing package version gitHead matches the exact requested release SHA; missing tag or GitHub Release finalization is recovered with the maintainer's authenticated local git and gh clients.</Recovery>
  </Publishing>
  <Rollback>
    <Expectation>Package rollback is managed by npm version selection and by rerunning vvoc sync or fixing vvoc.json manually when strict config validation blocks mutation.</Expectation>
  </Rollback>
</GraceDeployment>
